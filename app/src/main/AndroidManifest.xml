<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MusicApp">

        <!-- Player Activity -->
        <activity
            android:name="com.x2t68.MusicPlayer.presentation.PlayerActivity"
            android:exported="false" />

        <!-- Song List Activity (now correctly pointing to MainActivity class) -->
        <activity
            android:name="com.x2t68.MusicPlayer.presentation.MainActivity"
            android:exported="false"
            android:label="@string/title_activity_song_list"
            android:theme="@style/Theme.MusicApp" />

        <!-- Splash Activity (Launcher) -->
        <activity
            android:name="com.x2t68.MusicPlayer.presentation.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
